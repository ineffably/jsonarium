{
  "type": "class",
  "name": "Point",
  "sourcePath": "maths/point/Point.ts",
  "extends": null,
  "implements": [
    "PointLike"
  ],
  "description": "The Point object represents a location in a two-dimensional coordinate system, where `x` represents the position on the horizontal axis and `y` represents the position on the vertical axis. Many Pixi functions accept the `PointData` type as an alternative to `Point`, which only requires `x` and `y` properties. ```ts // Basic point creation const point = new Point(100, 200); // Using with transformations const matrix = new Matrix(); matrix.translate(50, 50).apply(point); // Point arithmetic const start = new Point(0, 0); const end = new Point(100, 100); const middle = new Point( (start.x + end.x) / 2, (start.y + end.y) / 2 ); ```",
  "category": "maths",
  "deprecated": false,
  "methods": [
    {
      "name": "clone",
      "params": [],
      "returnType": "Point",
      "description": "Creates a clone of this point, which is a new instance with the same `x` and `y` values. ```ts // Basic point cloning const original = new Point(100, 200); const copy = original.clone(); // Clone and modify const modified = original.clone(); modified.set(300, 400); // Verify independence console.log(original); // Point(100, 200) console.log(modified); // Point(300, 400) ``` - Creates new Point instance - Deep copies x and y values - Independent from original - Useful for preserving values"
    },
    {
      "name": "Point",
      "params": [
        {
          "name": "this.x",
          "type": null
        },
        {
          "name": "this.y",
          "type": null
        }
      ],
      "returnType": null,
      "description": null
    },
    {
      "name": "copyFrom",
      "params": [
        {
          "name": "p",
          "type": "PointData",
          "optional": false,
          "default": null
        }
      ],
      "returnType": "this",
      "description": "Copies x and y from the given point into this point. ```ts // Basic copying const source = new Point(100, 200); const target = new Point(); target.copyFrom(source); // Copy and chain operations const point = new Point() .copyFrom(source) .set(x + 50, y + 50); // Copy from any PointData const data = { x: 10, y: 20 }; point.copyFrom(data); ```"
    },
    {
      "name": "set",
      "params": [
        {
          "name": "p.x",
          "type": null
        },
        {
          "name": "p.y",
          "type": null
        }
      ],
      "returnType": null,
      "description": null
    },
    {
      "name": "equals",
      "params": [
        {
          "name": "p",
          "type": "PointData",
          "optional": false,
          "default": null
        }
      ],
      "returnType": "boolean",
      "description": "Copies this point's x and y into the given point. ```ts // Basic copying const source = new Point(100, 200); const target = new Point(); source.copyTo(target); ``` / public copyTo<T extends PointLike>(p: T): T { p.set(this.x, this.y); return p; } /** Checks if another point is equal to this point. Compares x and y values using strict equality. ```ts // Basic equality check const p1 = new Point(100, 200); const p2 = new Point(100, 200); console.log(p1.equals(p2)); // true // Compare with PointData const data = { x: 100, y: 200 }; console.log(p1.equals(data)); // true // Check different points const p3 = new Point(200, 300); console.log(p1.equals(p3)); // false ```"
    },
    {
      "name": "return",
      "params": [
        {
          "name": "p.x === this.x",
          "type": null
        }
      ],
      "returnType": null,
      "description": null
    },
    {
      "name": "set",
      "params": [
        {
          "name": "x = 0",
          "type": null
        },
        {
          "name": "y",
          "type": "number",
          "optional": true,
          "default": "x"
        }
      ],
      "returnType": "this",
      "description": "Sets the point to a new x and y position. If y is omitted, both x and y will be set to x. ```ts // Basic position setting const point = new Point(); point.set(100, 200); // Set both x and y to same value point.set(50); // x=50, y=50 // Chain with other operations point .set(10, 20) .copyTo(otherPoint); ```"
    },
    {
      "name": "toString",
      "params": [],
      "returnType": "string",
      "description": null
    },
    {
      "name": "set",
      "params": [
        {
          "name": "100",
          "type": null
        },
        {
          "name": "200",
          "type": null
        }
      ],
      "returnType": null,
      "description": null
    },
    {
      "name": "apply",
      "params": [
        {
          "name": "tempPoint",
          "type": null
        }
      ],
      "returnType": null,
      "description": null
    },
    {
      "name": "Point",
      "params": [],
      "returnType": null,
      "description": null
    }
  ],
  "properties": []
}